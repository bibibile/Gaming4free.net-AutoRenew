name: G4F Auto Renew

on:
  schedule:
    - cron: '23 16 * * *'  # 每天 0点, 8点, 16点的 23 分运行
  workflow_dispatch:

jobs:
  run-addtime:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 获取本仓库代码
        uses: actions/checkout@v4
        # 这里已经去掉了复杂的 token 和跨库拉取，直接读取本仓库的 main.py

      - name: 设置 Python 环境
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 安装 Python 依赖及系统组件
        run: |
          # 升级基础构建工具
          python -m pip install --upgrade pip setuptools wheel
          # 安装 DrissionPage 及其底层依赖
          pip install DrissionPage requests DataRecorder DownloadKit FlowViewer lxml websocket-client psutil
          # 安装虚拟显示器
          sudo apt-get update
          sudo apt-get install -y xvfb

      - name: 安装并配置 Cloudflare WARP CLI
        run: |
          curl -fsSL https://pkg.cloudflareclient.com/pubkey.gpg | sudo gpg --yes --dearmor --output /usr/share/keyrings/cloudflare-warp-archive-keyring.gpg
          echo "deb [signed-by=/usr/share/keyrings/cloudflare-warp-archive-keyring.gpg] https://pkg.cloudflareclient.com/ $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/cloudflare-client.list
          sudo apt-get update
          sudo apt-get install -y cloudflare-warp
          
          warp-cli --accept-tos registration new
          warp-cli --accept-tos mode warp
          warp-cli --accept-tos connect
          
          sleep 5
          echo "✅ WARP 初始化完成"

      - name: 运行续期脚本
        env:
          # 确保您的仓库 Secrets 里有这两个变量
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          xvfb-run -a python main.py
